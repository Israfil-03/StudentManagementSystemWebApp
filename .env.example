# Server Environment Variables
# -----------------------------
# The port the backend server will run on
PORT=3001

# PostgreSQL connection string
# Example for local development: DATABASE_URL="postgresql://user:password@localhost:5432/student_management?schema=public"
# Example for Azure: DATABASE_URL="postgresql://user:password@your-azure-host.postgres.database.azure.com:5432/student_management?schema=public&sslmode=require"
DATABASE_URL="postgresql://postgres:password@localhost:5432/student_management_db?schema=public"

# Secret key for signing JWTs. Should be a long, random string.
# Generate one with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET="your-super-secret-and-long-jwt-key"

# The origin URL for the frontend application for CORS policy.
# For local development with Vite default port
CORS_ORIGIN="http://localhost:5173"
# For a deployed frontend on Netlify (update with your actual URL)
# CORS_ORIGIN="https://your-app.netlify.app"

# Node environment (development | production)
NODE_ENV="development"


# Client Environment Variables (create client/.env file)
# -----------------------------
# The base URL of the backend API.
# For local development:
VITE_API_BASE_URL="http://localhost:3001/api"
# For production (your Azure App Service URL):
# VITE_API_BASE_URL="https://your-app.azurewebsites.net/api"

